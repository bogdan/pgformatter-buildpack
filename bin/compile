#!/bin/sh

BUILD_DIR=$1

# Change to the build directory
cd $BUILD_DIR

# Download and install pgFormatter
echo "-----> Installing pgFormatter"
git clone https://github.com/darold/pgFormatter.git
cd pgFormatter
perl Makefile.PL PREFIX=$BUILD_DIR/vendor
make && make install
export PATH=$BUILD_DIR/vendor/bin:$PATH

